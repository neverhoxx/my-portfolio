import { Container } from "@/components/shared/container";
import ContactForm from "@/components/shared/contact/contactform";

export default function Name() {
    return (
        <Container className='pt-40 flex items-center justify-center'>
            <ContactForm />
        </Container>
    );
}